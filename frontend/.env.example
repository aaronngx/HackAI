# Optional: Google Places API key for real doctor data, ratings, and opening hours.
# Get a key at https://console.cloud.google.com/ (enable Places API (New)).
# Without this, the app uses a fallback list with sample ratings.
GOOGLE_PLACES_API_KEY=
