# MongoDB connection (required for clinic database)
MONGODB_URI=

# Optional: Google API key for clinic photos, ratings, Distance Matrix (driving distance), and seeding.
# Enable: Places API, Distance Matrix API. Seed: POST /api/seed-clinics
GOOGLE_PLACES_API_KEY=
